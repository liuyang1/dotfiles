--gtest_color=[yes|no]
--gtest_repeat=[NUM]            -1, to repeat forever
--gtest_shuffle                 randoms sequence
--gtest_break_on_failure        stop when find failure
--gtest_filter=[case format]
--gtest_filter=*:-TestM2M.async     exclude one case, with - before pattern
--gtest_also_run_disabled_tests     run disabled test
