Force search all files (ignore .gitignore files)        <<< This is quite useful
ag -u [pattern]

print context

-A --after [LINES=2]		print lines After match
-B --before [LINES=2]		print lines Before match

list filename

ag -l
