- A build tool, like `make`.
- for embedded linux cross compilation, work with `yocto` and `OpenEmbedded` project.

bitbake -c clean [MODULE]
bitbake [MODULE]

# ref
doc: https://www.yoctoproject.org/docs/1.6/bitbake-user-manual/bitbake-user-manual.html
